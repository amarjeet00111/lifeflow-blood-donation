runtime: python312
env: standard

instance_class: F1

runtime_config:
  python_version: 3.12

entrypoint: gunicorn -b :$PORT app:app

handlers:
  - url: /static
    static_dir: static
  - url: /.*
    script: auto

env_variables:
  FLASK_ENV: production
